import React from 'react'

const HomePage = () => {
	return (
		<div>
			<div>I am the BEST hooome</div>
			<button onClick={() => console.log("presssssed")}>Press</button>
		</div>
	)
}

export default {
	component: HomePage
}